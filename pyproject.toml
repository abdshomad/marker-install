[project]
name = "marker-install"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.3",
    "marker-pdf",
    "python-multipart>=0.0.26",
    "uvicorn>=0.44.0",
]

[tool.uv.sources]
marker-pdf = { path = "marker", editable = true }
